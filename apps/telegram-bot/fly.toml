# fly.toml - E-Y Growth Bot (Telegram)

app = 'ey-growth-bot'
primary_region = 'ams'

[build]

# No HTTP service - this is a worker process using long polling
[processes]
  app = "npx tsx src/index.ts"

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
